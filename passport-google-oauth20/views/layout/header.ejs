<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Web Authentication</title>
  <style>
    body {
      margin: 0;
      font-family: 'Arial', sans-serif;
      background: linear-gradient(to right, #e96443, #904e95);
      color: #fff;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }
    header {
      width: 100%;
      background: rgba(0, 0, 0, 0.7);
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.5);
      padding: 1rem 0;
      position: fixed;
      top: 0;
      left: 0;
    }
    footer {
        margin-top: auto;
        width: 100%;
        background: linear-gradient(to right, #ff7eb3, #ff4896);
        text-align: center;
        padding: 1.5rem 0;
        color: #fff;
        font-size: 1.1rem;
        font-weight: bold;
        box-shadow: 0 -4px 10px rgba(0, 0, 0, 0.5);
        letter-spacing: 1px;
        border-top: 4px solid #ff1f6d;
    }

    footer p {
        margin: 0;
        animation: glow 2s infinite alternate;
    }
    nav {
      display: flex;
      justify-content: center;
      gap: 2rem;
    }

    nav a {
      text-decoration: none;
      font-size: 1.2rem;
      font-weight: bold;
      padding: 0.5rem 1rem;
      border-radius: 30px;
      transition: all 0.3s ease;
      background: #ff7eb3;
      color: #fff;
    }

    nav a:hover {
      background: hsl(63, 100%, 64%);
      color: #000;
      transform: scale(1.1);
      box-shadow: 0 10px 32px #5267ec80;
    }

    nav a:active {
      transform: scale(0.9);
      background: #ff1f6d;
    }

    main {
      margin-top: 5rem;
      text-align: center;
    }
    h1{
      font-size: 3rem;
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
      margin: 0;
    }
    h2 {
      font-size: 2.5rem;
      margin-bottom: 1rem;
    }

p {
  font-size: 1.2rem;
  max-width: 600px;
  line-height: 1.6;
  text-align: center;  /* Centers text inside the <p> */
  margin: 0 auto;      /* Centers the <p> block within its parent */
}

      button {
        font-size: 1.1rem;
        padding: 0.7rem;
        border: none;
        border-radius: 30px;
        background: hsl(222, 77%, 61%);
        color: rgb(236, 229, 89);
        /* color:#ff7e5f */
        cursor: pointer;
        transition: all 0.3s ease;
      }

      button:hover {
        transform: scale(1.1);
        box-shadow: 4px 6px 8px rgba(0, 0, 0, 0.3);
        background: hsl(99, 100%, 64%);
        color: #24298c;
      }

      button:active {
        transform: translateY(2px);
      }
            label {
        font-size: 1rem;
        font-weight: bold;
        font-style: inherit;
      }
        .google-btn {
                background: linear-gradient(45deg, #4285F4, #EA4335, #FBBC05, #34A853); 
                color: white; 
                padding: 10px 20px; 
                border-radius: 8px; 
                text-decoration: none; 
                font-weight: bold; 
                display: inline-block; 
                transition: 0.3s;
        }

        .google-btn:hover {
            transform: scale(1.05);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.3);
        }
  </style>
</head>

<body>
  <header>
    <nav>
      <a href="/">Home</a>
      <!-- <a href="/register">Register</a> -->
      <a href="/login">Login</a>
      <a href="/profile">Profile</a>
      <a href="/logout">Logout</a>
    </nav>
  </header>
  <main>
    <!-- <h1>Welcome to Our Website</h1> -->
  </main>
</body>

</html>